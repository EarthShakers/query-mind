# QueryMind — AI 数据查询助手

> 让每个人都能用自然语言查询数据库

---

## 一句话定位

**QueryMind 是一款面向非技术用户的 AI 数据查询工具，用户说人话，系统自动生成 SQL、执行查询、智能选择表格或图表展示结果。**

---

## 核心卖点

### 1. 零 SQL 门槛
业务人员、运营、管理层无需学习 SQL，直接用自然语言提问。
- "上个月华南区卖了多少？" → 自动生成 SQL → 返回结果
- 降低数据分析的人力依赖，减少对数据分析师的重复性需求

### 2. AI 自动选择可视化方式
不是简单的"文字回复"。AI 根据问题语义，自动判断：
- 查明细 → 表格
- 看趋势 → 折线图
- 做对比 → 柱状图
- 看占比 → 饼图

**用户不需要指定图表类型，AI 替你决定最合适的展示方式。**

### 3. 流式响应，秒级反馈
基于 Streaming 架构，AI 生成过程实时推送到前端，不需要等整个回答生成完毕。用户发出问题后 1-2 秒即可看到第一个 token，体验接近实时对话。

### 4. 结构化输出，100% 可靠
通过 Zod Schema 强制约束 AI 输出格式，杜绝 JSON 解析失败、字段缺失等问题。每一次查询结果都是结构化、可预期的。

### 5. 轻量部署，即插即用
- 无需复杂的 BI 系统搭建
- 对接企业现有数据库（MySQL / PostgreSQL / SQLite）
- Docker 一键部署，10 分钟上线

---

## 目标用户

| 角色 | 痛点 | QueryMind 价值 |
|------|------|----------------|
| **业务运营** | 每次看数据都要找数据分析师写 SQL | 自己直接问，秒出结果 |
| **管理层** | 看不懂 BI 报表，想要即时数据 | 用自然语言问，AI 自动可视化 |
| **数据分析师** | 被重复性取数需求占满时间 | 把简单查询交给 AI，聚焦高价值分析 |
| **中小企业** | 没预算上 Tableau / PowerBI | 低成本替代方案 |

---

## 竞品分析

| 维度 | QueryMind | Tableau Ask Data | ChatBI（百度） | Metabase |
|------|-----------|------------------|---------------|----------|
| **交互方式** | 自然语言对话 | 自然语言 | 自然语言 | 拖拽 + SQL |
| **可视化自动选择** | AI 自动决定 | 有限支持 | 支持 | 手动配置 |
| **流式响应** | 支持 | 不支持 | 部分支持 | 不适用 |
| **部署成本** | 极低（单容器） | 高（需 Tableau Server） | SaaS 绑定 | 中等 |
| **定价** | 按查询量计费 | $70/用户/月起 | 按 token 计费 | 开源免费 / 企业版 |
| **数据库支持** | MySQL/PG/SQLite | Tableau 数据源 | 百度生态 | 多数据库 |
| **私有化部署** | 支持 | 企业版支持 | 不支持 | 支持 |
| **AI 模型灵活性** | 可切换任意模型 | 内置不可替换 | 文心大模型 | 无 AI |

### 差异化优势

1. **vs Tableau Ask Data**：部署成本低 10 倍，不需要 Tableau 生态绑定
2. **vs ChatBI**：支持私有化部署，模型可替换，不依赖单一云厂商
3. **vs Metabase**：无需学习拖拽建图，纯对话式交互，上手成本为零
4. **vs 直接用 ChatGPT**：ChatGPT 无法连接企业数据库，QueryMind 直连数据源执行真实查询

---

## 产品形态

### SaaS 版
- 注册 → 连接数据库 → 开始提问
- 按查询次数阶梯计费
- 适合中小团队快速上手

### 私有化部署版
- Docker / K8s 部署到客户内网
- 数据不出企业网络
- 支持对接企业自有 LLM（如通义千问、DeepSeek、私有 GPT）
- 适合金融、医疗、政务等数据敏感行业

---

## 商业模式

| 版本 | 定价 | 包含内容 |
|------|------|----------|
| **免费版** | ¥0 | 每天 20 次查询，单数据库，社区支持 |
| **专业版** | ¥299/月 | 无限查询，多数据库，图表导出，历史记录 |
| **团队版** | ¥999/月 | 10 人协作，权限管理，共享看板，API 接口 |
| **企业版** | 按需报价 | 私有化部署，自定义模型，专属技术支持，SLA 保障 |

---

## 技术壁垒

1. **Prompt Engineering 积累**：针对不同数据库 schema 的 SQL 生成准确率优化
2. **智能可视化决策引擎**：不只是调 AI，还有基于数据特征的可视化类型推荐逻辑
3. **多轮对话上下文管理**：支持追问（"换成饼图看看"、"只看华南区的"）
4. **企业级安全**：SQL 注入防护、只读权限控制、查询审计日志

---

## 路线图

### Phase 1 — MVP（当前）
- [x] 自然语言 → SQL → 表格/图表
- [x] 流式响应
- [x] 多图表类型自动选择

### Phase 2 — 产品化
- [ ] 多数据库连接（MySQL / PostgreSQL）
- [ ] 查询历史与收藏
- [ ] 图表导出（PNG / PDF）
- [ ] 多轮追问优化

### Phase 3 — 商业化
- [ ] 团队协作与权限管理
- [ ] 定时报表与邮件推送
- [ ] 自定义 Dashboard
- [ ] API 开放平台

### Phase 4 — 生态
- [ ] 插件市场（自定义可视化组件）
- [ ] 与飞书/钉钉/企业微信集成
- [ ] 数据血缘与影响分析
